import { MarvellousCheckPipe } from './marvellous-check.pipe';

describe('MarvellousCheckPipe', () => {
  it('create an instance', () => {
    const pipe = new MarvellousCheckPipe();
    expect(pipe).toBeTruthy();
  });
});
